class Page extends Component {
  constructor(props) {
    super(props);
    this.state = { 
      byteSize = props.size,
      number = props.number,
      
    }
  }
  render() { 
    return 0;
  }
}

export default Page;